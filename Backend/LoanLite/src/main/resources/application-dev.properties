# Development profile - local database and verbose SQL logging
spring.datasource.url=jdbc:postgresql://localhost:5432/LoanLite

# Enable detailed SQL logging in dev
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# JWT settings
jwt.secret=change-me-in-prod
jwt.expiration-ms=3600000