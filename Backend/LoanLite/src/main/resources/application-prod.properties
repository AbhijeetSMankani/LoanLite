# Production profile - replace with your production DB URL
spring.datasource.url=jdbc:postgresql://<PROD_HOST>:5432/LoanLite

# Disable SQL logging in production
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false
